# Scrolling Tiles Demo

This is a parallax scrolling tiles demo using the `<canvas />` element.

### Build and run in dev mode

```
npm start
```

Point your browser to http://localhost:3000

### Create a deployable build

```
npm run build
```
